# ── API runtime dependencies ──────────────────────────────────────────────────
# Selastone Loan Approval System
# Compliance: Explainable | Auditable | Policy-aligned | Privacy-compliant
# Used by Dockerfile.api — excludes notebook, Windows, and dev packages

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn==0.48.0
starlette==1.1.0
python-multipart==0.0.32

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0

# ── ML + Explainability (SHAP for compliance) ─────────────────────────────────
xgboost==3.2.0
lightgbm==4.6.0
scikit-learn==1.8.0
numpy==2.4.6
pandas==2.3.3
joblib==1.5.3
scipy==1.17.1
threadpoolctl==3.6.0
shap==0.52.0                # Explainability — SHAP values per prediction

# ── Storage / messaging ───────────────────────────────────────────────────────
celery==5.4.0              # Async batch scoring worker — broker: Redis
redis==8.0.0
clickhouse-connect==0.8.10  # Auditability — prediction audit logs
minio==7.2.15               # Privacy — batch upload to isolated tenant storage

# ── MLflow + S3/MinIO artefact store ─────────────────────────────────────────
mlflow==3.13.0              # Auditability — model versioning + registry
boto3==1.43.41               # MLflow S3 backend — >=1.34 compatible with urllib3 2.x
botocore==1.43.41            # boto3 dependency — urllib3 2.x compatible
s3transfer==0.19.0

# ── RAG ingestion (rag_harness) ───────────────────────────────────────────────
pdfplumber==0.11.10          # PDF text extraction
pdfminer.six==20260107       # pdfplumber dependency
pypdfium2==5.13.0            # pdfplumber dependency
python-docx==1.2.0           # DOCX text extraction
lxml==6.1.3                  # python-docx dependency
tiktoken==0.14.0             # token counting for the 400/50-token split/merge thresholds
regex==2026.9.3              # tiktoken dependency
openai==3.8.0                # text-embedding-3-small — see rag_harness/ingest_task.py for the tradeoff vs. a local model
httpx2==2.12.0               # openai dependency (its own httpx fork, unrelated to httpx above)
httpcore2==2.12.0            # httpx2 dependency
jiter==0.16.0                # openai dependency
truststore==0.10.4           # openai dependency

# ── HTTP ──────────────────────────────────────────────────────────────────────
httpx==0.28.1
httpcore==1.0.9
requests==2.34.2

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-client==0.25.0   # Policy-aligned — quota + latency metrics for Grafana

# ── Auth (user accounts, JWT sessions, API keys) ─────────────────────────────
psycopg2-binary==2.9.10     # Postgres driver — app.users / app.tenants / app.api_keys
PyJWT==2.13.0
bcrypt==4.2.1               # password hashing — used directly, not via passlib
email-validator==2.3.0      # pydantic EmailStr

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
typing_extensions==4.15.0
anyio==4.13.0
sniffio==1.3.1
certifi==2026.5.20
charset-normalizer==3.4.7
idna==3.19                  # bumped by openai's anyio pin — was 3.16
urllib3==2.7.0
packaging==26.2