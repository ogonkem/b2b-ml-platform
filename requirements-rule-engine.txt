# ── rule_engine service dependencies ──────────────────────────────────────────
# Deliberately minimal — no ML/RAG stack (xgboost, pdfplumber, openai, ...).
# Just enough to run a thin FastAPI wrapper around rule_engine/engine.py's
# pure-Python decision logic and reuse app.auth.verify_token. Versions copied
# from requirements.txt to stay in lockstep with the rest of the project —
# bump both together if either changes.

fastapi==0.136.3
uvicorn==0.48.0
starlette==1.1.0
python-multipart==0.0.32

pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0

# app.auth's own dependencies
psycopg2-binary==2.9.10
PyJWT==2.13.0
bcrypt==4.2.1
email-validator==2.3.0
