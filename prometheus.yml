# prometheus.yml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "selastone-api"
    static_configs:
      - targets: ["selastone-api:8000"]
    metrics_path: "/metrics"